SELECT ${metric}
FROM ${sample}
WHERE (
  hostname = '${instance_id}'
  AND processDisplayName LIKE 'newrelic-agent-control%'
  %{ for k, v in wheres }
  AND ${k}='${v}'
  %{ endfor }
)
%{ if since != "" }
SINCE ${since} seconds ago
%{ endif }
