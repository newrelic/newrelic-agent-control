# Cluster and License info is set with "--set" in the e2e-spec file
# global:
#   cluster: ""
#   licenseKey: ""

super-agent-deployment:
  config:
    subAgents:
      open-telemetry:
        content:
          chart_values:
            verboseLog: true
