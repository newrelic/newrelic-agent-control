SELECT count(*)
FROM Log
WHERE cluster_name = '${instance_id}'
AND service.name = 'agent-control-self-instrumentation'
AND (
  level = 'ERROR'
  OR message LIKE '%exited unsuccessfully%'
  OR message LIKE '%Restart policy exceeded%'
)
