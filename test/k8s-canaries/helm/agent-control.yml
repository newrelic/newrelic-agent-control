# Cluster and License info is set with "--set" in the e2e-spec file
<<<<<<< HEAD
agent-control-deployment:
  image:
    pullPolicy: Always
  config:
    log:
      level: debug
    override:
      self_instrumentation:
        # most of the otel config is set in the makefile
        opentelemetry:
          traces:
            enabled: true
    agents:
=======
agentControlDeployment:
  chartValues:
    image:
      pullPolicy: Always
    config:
      log:
        level: debug
      override:
        self_instrumentation:
          opentelemetry:
            traces:
              enabled: true
      agents:
        infra:
          agent_type: newrelic/com.newrelic.infrastructure:0.1.0
        prometheus:
          agent_type: newrelic/com.newrelic.prometheus:0.1.0
        logs:
          agent_type: newrelic/io.fluentbit:0.1.0
    agentsConfig:
>>>>>>> 337fd92d (chore: adapt chart values to new structure)
      infra:
        chart_version: "*" # Use latest
      prometheus:
        chart_version: "*" # Use latest
      logs:
        chart_version: "*" # Use latest
