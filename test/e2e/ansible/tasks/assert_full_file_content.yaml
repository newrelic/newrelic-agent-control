---
- name: assert on config content
  shell: cat {{ file_path }}
  register: file_content

- name: assert on content
  assert:
    that: "{{ file_content.stdout | trim  == expected_content | trim  }}"
    msg: "{{ file_path }} doesn't have expected content '{{ file_content.stdout }}' != '{{ expected_content }}'"

...
