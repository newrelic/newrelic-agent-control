namespace: newrelic
name: com.newrelic.infrastructure.nri_redis
version: 0.1.0
platform: host
operating_system: linux
protocol_version: "1.0"
variables:
  config_integration:
    description: "nri-redis integration YAML consumed by the infra-agent"
    type: yaml
    required: true
  version:
    description: "OCI package version for nri-redis"
    type: string
    required: true
  oci:
    repository:
      description: "OCI repository name for nri-redis"
      type: string
      required: false
      default: newrelic/newrelic-agent-control-redis-dev
      variants:
        ac_config_field: "oci_repository_urls"
        values: [ "newrelic/newrelic-agent-control-redis-dev" ]
deployment:
  packages:
    nri-redis:
      download:
        oci:
          repository: ${nr-var:oci.repository}
          version: ${nr-var:version}
          public_key_url: https://publickeys.newrelic.com/g/agent-control-oci/global/nri-redis/jwks.json
  shared_filesystem:
    infra-agent-ohi-configs:
      kind: dir
      entries:
        nri-redis.yaml:
          kind: file
          text: ${nr-var:config_integration}
    infra-agent-ohi-binaries:
      kind: dir
      entries:
        nri-redis:
          kind: file
          copy_from_file: ${nr-sub:packages.nri-redis.dir}/nri-redis
