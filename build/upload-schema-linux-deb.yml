---
- src: "nr-meta-agent_{version}_linux_{arch}.deb"
  arch:
    - amd64
    - arm64
  uploads:
    - type: apt
      src_repo: "{access_point_host}/limited_preview/linux/apt"
      dest: "{dest_prefix}linux/apt/"
      os_version:
        - jammy
        - focal
        - bionic
        - xenial
        - bullseye
        - buster

#########################
  # UPLOAD CHECKSUMS #
#########################

- src: "nr-meta-agent_{version}_linux_{arch}.deb.sum"
  arch:
    - amd64
    - arm64
  uploads:
    - type: file
      dest: "{dest_prefix}linux/apt/pool/main/n/nr-meta-agent/{src}"
