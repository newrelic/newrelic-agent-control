name: 📞 Test agent package installation

on:
  workflow_call:
    inputs:
      TAG:
        required: true
        type: string
      PACKAGE_NAME:
        required: true
        type: string
      REPO_ENDPOINT:
        required: false
        type: string
        description: 'Repository endpoint to fetch packages from'
        default: "https://download.newrelic.com/preview"

permissions:
  contents: read

env:
  TAG: ${{ inputs.TAG }}
  REPO_ENDPOINT: ${{ inputs.REPO_ENDPOINT }}

jobs:
  molecule-test-packages:
    name: Test package installation
    runs-on: ubuntu-latest
    steps:
      - uses: newrelic/pkg-installation-testing-action@be72c7ec6239d36b03d9b2b1cdc997ea024866e8 # v1
        with:
          gpg_key: 'https://download.newrelic.com/infrastructure_agent/gpg/newrelic-infra.gpg'
          repo_base_url: ${{ inputs.REPO_ENDPOINT }}
          package_name: ${{ inputs.PACKAGE_NAME }}
          package_version: ${{ inputs.TAG }}
          version_command: "version"
          platforms: "al2023,centos8,debian-bullseye,debian-bookworm,redhat8,redhat9,suse15.3,suse15.4,suse15.5,ubuntu2004,ubuntu2204,ubuntu2404,ubuntu2604"
